name: publish

on: [push]

jobs:
  publish-hello-world-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the hello-world Docker image
      run: |
        docker login ghcr.io -u 062995 --password $token
      env:
        CR_PAT: ${{ secrets.token }}
